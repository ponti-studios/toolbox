{
  "name": "agentkit",
  "version": "0.1.0",
  "description": "Unified AI agent usage & cost CLI — Claude Code, Codex, Copilot, OpenRouter, OpenCode",
  "keywords": [
    "ai",
    "analytics",
    "cli",
    "cost",
    "llm",
    "usage"
  ],
  "license": "MIT",
  "bin": {
    "agentkit": "./dist/index.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "test": "node --experimental-strip-types --test src/**/*.test.ts"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=24"
  }
}
